import React from "react";

const Calendar = () => {

    //Click on date -> tasks for that date + back to calendar button
    return (
        <div>
            This is the calendar
        </div>
    )
}

export default Calendar;